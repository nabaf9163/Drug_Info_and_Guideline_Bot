rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access is via Firebase Admin SDK (server-side),
    // which bypasses these rules. Lock down all client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
